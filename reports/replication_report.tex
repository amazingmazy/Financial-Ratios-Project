% ---------------------------------------------------------------------------
% replication_report.tex
%
% The project's write-up. Skeleton at this stage: the structure and the
% build chain are in place and compile, but the discussion sections and our own
% exhibits are still to be written.
%
% Deliberately plain `article` rather than the scaffold's my_article_header.sty.
% That header currently fails to compile -- `\newtheorem{mycorollary}` in
% my_common_header.sty raises "Command \c@mylemma already defined" -- and this
% document needs none of the theorem machinery it provides. Worth revisiting if
% we later want the scaffold's styling.
%
% Every number and figure here is generated by code and pulled in with \input or
% \includegraphics. Nothing is typed by hand, so `doit` regenerates the whole
% document from the raw pulls.
%
% Build:  doit compile_latex_docs      (or: latexmk -xelatex -cd reports/replication_report.tex)
% ---------------------------------------------------------------------------
\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{caption}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\usepackage{float}
\usepackage{placeins}

% Generated artefacts live in _output/, resolved relative to this file so the
% build works from any working directory.
\newcommand{\PathToOutput}{../_output}
\graphicspath{{../_output/}}

\title{Predicting Returns with Financial Ratios:\\
       A Replication and Extension of Lewellen (2004)}
\author{Anthony Mazy \and Stefano Ramponi}
\date{\today}

\begin{document}
\maketitle

% Prose macros -- generated by the same pipeline runs that produce the
% tables below, so numbers quoted in text can't drift from the adjacent
% table (see dodo.py: task_tables_siz, task_tables_ciz, task_schema_compare_macros).
\input{\PathToOutput/macros_table2_siz.tex}
\input{\PathToOutput/macros_table_ext_1946_ciz.tex}
\input{\PathToOutput/macros_table_ext_2001_ciz.tex}
\input{\PathToOutput/macros_schema_compare.tex}
\input{\PathToOutput/macros_own_exhibits.tex}

\begin{abstract}
We replicate Lewellen (2004), which argues that the standard Stambaugh (1999)
small-sample bias correction understates the predictive power of financial
ratios, and that imposing the economically motivated bound $\rho \le 1$ yields
substantially stronger evidence of return predictability. We reproduce the
paper's dividend-yield results on CRSP data, extend the sample past its 2000
cutoff, and report where our numbers diverge and why.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Lewellen (2004) revisits a long-standing question in empirical finance:
whether financial ratios like the dividend yield (\(DY\)), book-to-market
(\(B/M\)), and earnings-price ratio (\(E/P\)) can predict aggregate stock
returns. Predictive regressions of this kind are known to suffer from a
small-sample bias, first characterized by Stambaugh (1986, 1999), that
arises because the predictor's own innovations are strongly correlated with
the return's innovations. The standard correction for this bias --
Stambaugh's marginal, or unconditional, small-sample distribution of the
slope estimate -- had become the accepted way to test for predictability,
and typically found weak evidence at best.

The paper's contribution is to show that this standard correction discards
useful information. Because \(DY\), \(B/M\), and \(E/P\) are all highly
persistent, stationarity itself puts an upper bound on their true
autocorrelation \(\rho\): it must be less than one. Conditioning on this
bound -- rather than integrating over all possible values of \(\rho\), as
the unconditional approach does -- produces a bias-adjusted estimator with a
closed-form Student-\(t\) distribution and a substantially smaller standard
error. Under the conservative assumption \(\rho \approx 1\), Lewellen finds
that dividend yield predicts NYSE returns strongly and consistently across
1946--2000 and its subperiods, in cases where the unconditional test finds
essentially nothing.

This project replicates the paper's central results -- the AR(1) regression
for \(\log(DY)\) and the three competing predictive-regression estimators
(OLS, Stambaugh, and the \(\rho \approx 1\) conditional test) -- on real
CRSP and Compustat data, extends the sample through the present, and
reports where our numbers agree with the paper, where they diverge, and
why. Section~\ref{sec:data} describes the data sources and construction.
Section~\ref{sec:replication} compares our replicated tables against the
paper's published values. Section~\ref{sec:extension} extends the sample to
2025 and discusses how the paper's own stated conditions for the
conditional test's power play out in the more recent data.
Section~\ref{sec:exhibits} presents our own summary statistics and figures.
Section~\ref{sec:conclusion} discusses successes and open issues.

\section{Data}
\label{sec:data}

Returns, the risk-free rate, and index levels come from CRSP; book equity
and operating income come from Compustat, linked to CRSP via the CRSP/Compustat
Merged (CCM) linktable. We restrict the CRSP universe to NYSE-listed
securities throughout, using an exchange-code-filtered index rather than
CRSP's off-the-shelf composite index, since the latter is not NYSE-only
after NASDAQ's addition to CRSP's composite series in December 1972.

\paragraph{Dividend yield.} Following the paper (Section 3), we construct
the monthly dividend flow as \(\text{Div}_t \approx (\text{vwretd}_t -
\text{vwretx}_t) \times \text{TotVal}_{t-1}\), roll it to a trailing
12-month sum, and divide by the \emph{current} month's aggregate NYSE
market value to obtain \(DY_t\). All predictive regressions use
\(\log(DY)\) rather than the raw ratio, matching the paper's own choice:
raw \(DY\) is mechanically positively skewed and its volatility scales with
its level, while the log series is close to normal and better behaved for
the AR(1) specification the estimators assume.

\paragraph{Book-to-market and earnings-price ratios.} \(B/M\) and \(E/P\)
are constructed by aggregating Compustat book equity and operating income
before depreciation across all NYSE-listed, Compustat-covered firms with at
least three years of accounting history, grouped by fiscal year rather than
by exact fiscal year-end date (grouping by date scatters a single fiscal
year's cross-section of firms across dozens of near-single-firm groups,
since firms' fiscal year-ends differ). Accounting data is lagged at least
four months before being matched to a calendar month, so that no regression
ever uses information before it would have been publicly available.

Book equity itself admits more than one convention. We follow the
Fama-French definition, \(\text{CEQ} + \text{TXDITC} - \text{preferred
stock}\), as a starting point, but found that including deferred taxes
(\(\text{TXDITC}\)) inflates our aggregate \(B/M\) roughly 10\% above the
paper's published mean; dropping \(\text{TXDITC}\) closes nearly all of the
gap (Section~\ref{sec:replication}). Since Lewellen's paper specifies only
``the ratio of book equity to market equity'' without giving a formula, and
since the deferred-tax treatment is not settled even within the Fama-French
literature (it changed after FASB 109), we treat this as a documented,
deliberate choice rather than a bug, and default to excluding deferred
taxes as the variant that reproduces the paper.

\paragraph{Two CRSP schemas.} CRSP's legacy tables (SIZ) are frozen at
2024-12-31; the current Flat File 2.0 schema (CIZ), which replaced it in
January 2025, is the only one that reaches the present, but compounds
returns with dividends reinvested on the ex-date rather than SIZ's
month-to-month holding-period convention. We build both panels rather than
splicing them into a single series: the replication is run on SIZ, which
reproduces the paper's own return-compounding convention most closely, and
the extension to the present is run on CIZ, which is the only schema
capable of reaching it.

\paragraph{QA gate.} Before any predictive regression is run, the
constructed panel is checked against the paper's own Table 1 (mean,
standard deviation, skewness, and lag-1/12/24 autocorrelations for every
series). This QA step, not the predictive regressions themselves, is what
surfaced the data-construction issues discussed below.

\section{Replication}
\label{sec:replication}

We reproduce Tables 1 through 6 using the shared estimator library
(\texttt{src/estimators.py}): OLS, Stambaugh's (1999) Monte Carlo
bias-adjusted estimator, and Lewellen's closed-form \(\rho \approx 1\)
conditional test, together with the modified-Bonferroni joint test.

\paragraph{Dividend yield (Tables 1--3) replicates cleanly.} On the full
1946--2000 sample, our AR(1) regression for \(\log(DY)\) and the three
predictive-regression estimators for VWNY match the paper closely:

\begin{table}[H]
\centering
\input{\PathToOutput/table2_siz.tex
}
\caption{Table 2 (dividend yield predicts NYSE returns, 1946--2000, SIZ
panel): OLS, Stambaugh (1999), and the \(\rho \approx 1\) conditional test,
for nominal and excess VWNY/EWNY. Compare the \(\rho \approx 1\) row's
\(\hat b = \TableTwoOursRhoOneB\) (\(t = \TableTwoOursRhoOneT\)) against the
paper's published \(0.663\) (\(t = 4.67\)) for VWNY -- the OLS standard
error (\(\TableTwoOursOLSse\) vs.\ the paper's published \(0.476\)) is the
single strongest indicator that the underlying dividend-flow and index
construction is correct.}
\label{tab:table2-compare}
\end{table}

The OLS standard error matching to three decimal places is the strongest
single indicator that the index and dividend-flow construction is correct,
since it depends on both the variance of \(\log(DY)\) and the sample being
the same 660 months as the paper's. We formalize this comparison as an
automated test suite (\texttt{tests/test\_replication\_vs\_paper.py}, 320
parametrized assertions against Lewellen's published Tables 1, 2, 3, 5, and
6, at tolerances fixed \emph{before} results were seen and set according to
how each statistic propagates into the final estimates -- autocorrelation
is gated tightly, since a 0.01 error in \(\hat\rho\) can move the
bias-adjusted slope by roughly 0.9 and flip the sign of the paper's
headline result).

The subperiod split (Table 3) repeats the same regressions separately for
1946--1972 and 1973--2000, the robustness check the paper uses to show the
full-sample result is not carried by one unusual decade:

\begin{table}[H]
\centering
\input{\PathToOutput/table3a_siz.tex}
\caption{Table 3a (dividend yield predicts NYSE returns, 1946--1972, SIZ
panel). The paper finds strong predictability in this subperiod despite the
shorter sample -- the \(\rho \approx 1\) row is the one to check against
that claim.}
\label{tab:table3a}
\end{table}

\begin{table}[H]
\centering
\input{\PathToOutput/table3b_siz.tex}
\caption{Table 3b (dividend yield predicts NYSE returns, 1973--2000, SIZ
panel). Compare \(\hat\rho\) against Table 3a's: the paper's argument for
why the conditional test dominates depends on both subperiods staying close
to a unit root, not just the full sample.}
\label{tab:table3b}
\end{table}

\paragraph{Sensitivity to the 1995--2000 run-up (Table 4).} The paper's
Section 4.2 shows that dropping the last five years of the sample roughly
doubles the OLS slope on \(\log(DY)\) while leaving the \(\rho \approx 1\)
estimate nearly unchanged, because the same run-up that inflates the OLS
coefficient also pushes \(\hat\rho\) higher, which the conditional test's
bias term absorbs. Table 4 repeats that comparison on our data,
1946--1994 against 1946--2000:

\begin{table}[H]
\centering
\input{\PathToOutput/table4a_siz.tex}
\caption{Table 4a (dividend yield predicts NYSE returns, 1946--1994, SIZ
panel, nominal returns only, per the paper).}
\label{tab:table4a}
\end{table}

\begin{table}[H]
\centering
\input{\PathToOutput/table4b_siz.tex}
\caption{Table 4b (dividend yield predicts NYSE returns, 1946--2000, SIZ
panel, repeated from Table 2 for direct comparison against Table 4a). The
OLS row should move noticeably between the two tables; the \(\rho \approx
1\) row should not -- that stability is the paper's point about the
conditional test absorbing the run-up through \(\hat\rho\) rather than
through the point estimate.}
\label{tab:table4b}
\end{table}

\paragraph{Book-to-market and earnings-price ratios (Tables 5--6) required
resolving a real definitional ambiguity.} Our initial pull of \(B/M\)
averaged 58.69 over 1963--2000 against the paper's published 53.13, a
10.5\% gap; \(E/P\) diverged about half as much. Tracing the asymmetry --
an error in the firm-level accounting pull should hit both ratios roughly
equally, while an error specific to the book-equity \emph{formula} should
hit only \(B/M\) -- pointed to deferred taxes (\(\text{TXDITC}\)), which
make up 12.1\% of our aggregate book equity under the standard
Fama-French definition. Excluding them brings the mean to 52.13, within
2\% of the paper, though a residual gap of roughly 5\% remains in both
ratios, attributable to the paper's unspecified ``three years of accounting
history'' screen and 20+ years of subsequent Compustat data restatement.
Per the assigned scope, \(B/M\) (Table 5) is the designated deliverable;
\(E/P\) (Table 6) is carried as a robustness check.

\begin{table}[H]
\centering
\input{\PathToOutput/table5a_siz.tex}
\caption{Table 5a (book-to-market predicts NYSE returns, June
1963--Dec\ 1994, SIZ panel, deferred taxes excluded from book equity). The
paper reports significant predictability for nominal returns only in this
subperiod; compare the OLS and \(\rho \approx 1\) \(p\)-value rows for
nominal vs.\ excess VWNY/EWNY.}
\label{tab:table5a}
\end{table}

\begin{table}[H]
\centering
\input{\PathToOutput/table5b_siz.tex}
\caption{Table 5b (book-to-market predicts NYSE returns, June
1963--Dec\ 2000, SIZ panel). The paper finds \(B/M\) predicts the
equal-weighted index once 1995--2000 is included but loses power for
VWNY -- worth checking whether that same asymmetry between EWNY and VWNY
shows up here.}
\label{tab:table5b}
\end{table}

\begin{table}[H]
\centering
\input{\PathToOutput/table6a_siz.tex}
\caption{Table 6a (earnings-price ratio predicts NYSE returns, June
1963--Dec\ 1994, SIZ panel). Carried as a robustness check on Table 5;
identical machinery with \(\log(E/P)\) in place of \(\log(B/M)\).}
\label{tab:table6a}
\end{table}

\begin{table}[H]
\centering
\input{\PathToOutput/table6b_siz.tex}
\caption{Table 6b (earnings-price ratio predicts NYSE returns, June
1963--Dec\ 2000, SIZ panel). \(E/P\)'s underlying data passed the Table 1
QA gate cleanly with no coverage-ramp caveat, unlike \(B/M\), so this
table's numbers do not carry the same residual-gap discussion as Table 5.}
\label{tab:table6b}
\end{table}

\section{Extension}
\label{sec:extension}

Extending the sample requires the CIZ schema, since CRSP's legacy tables do
not reach past 2024. On the full extended sample, \textbf{the paper's
conclusion survives}: for \ExtFullWindow{} (VWNY), OLS gives \(\hat b =
\ExtFullOLSb\) (\(p = \ExtFullOLSp\)), Stambaugh's unconditional correction
gives \(\hat b = \ExtFullStambB\) (\(p = \ExtFullStambP\)), and the \(\rho
\approx 1\) conditional test gives \(\hat b = \ExtFullRhoOneB\) (\(t =
\ExtFullRhoOneT\), \(p = \ExtFullRhoOneP\)) -- the same qualitative pattern
the paper is built around, roughly twenty-five years past its original
cutoff: the unconditional test fails to reject while the conditional test
rejects decisively.

\begin{table}[H]
\centering
\input{\PathToOutput/table_ext_1946_ciz.tex}
\caption{Extension, \ExtFullWindow{} (CIZ panel): the full sample carried
past the paper's original cutoff. Compare the OLS and \(\rho \approx 1\)
\(p\)-value rows -- the pattern the paper is built around, Stambaugh
failing to reject where the conditional test rejects decisively, should
still be visible here.}
\label{tab:table-ext-1946}
\end{table}

Splitting the extension at 2001 is more interesting, and is not a
counterexample to the paper. Dividend yield's autocorrelation falls to
\(\ExtRecentRhoHat\) in the post-2000 subsample, below the paper's own
stated threshold (Section 2.4) of roughly \RhoThreshold{} for a 25-year monthly sample
-- and the conditional test's power collapses accordingly: \(\hat b =
\ExtRecentRhoOneB\) (\(t = \ExtRecentRhoOneT\), \(p = \ExtRecentRhoOneP\)),
even as OLS reports \(\hat b = \ExtRecentOLSb\) (\(p = \ExtRecentOLSp\)).
This is exactly the relationship Lewellen's own Table A.1 power simulation
predicts: the conditional test's power falls toward zero as \(\rho\) drops
away from one, and the bias-adjusted estimate is biased downward once
\(\rho\) is genuinely below the assumed 1. Rather than contradicting the
paper, the post-2000 subsample demonstrates the limitation the paper itself
states, and the autocorrelation is the number to read first when
interpreting it -- the OLS slope moving from \(\ExtFullOLSb\) over the full
extended sample to \(\ExtRecentOLSb\) over the shorter, less-persistent
post-2000 window, with a stable low \(p\)-value throughout, is the
small-sample bias the paper is about, made visible by the shorter sample
rather than by any change in the underlying economics.

\begin{table}[H]
\centering
\input{\PathToOutput/table_ext_2001_ciz.tex}
\caption{Extension, 2001--present (CIZ panel): data the paper never saw.
The low \(\hat\rho\) row is the number to read first -- it sits below the
paper's own stated power threshold for a 25-year sample, so the \(\rho
\approx 1\) row's loss of significance here is the paper's stated
limitation playing out, not a contradiction of its result.}
\label{tab:table-ext-2001}
\end{table}

As a robustness check, we also compare the SIZ and CIZ schemas over an
identical \SchemaWindow{} window (truncating CIZ to match SIZ's endpoint,
so the comparison is not confounded by sample length). The two schemas'
results differ modestly -- e.g.\ \(\text{corr}(e,m)\) of
\(\SchemaSIZCorrEM\) (SIZ) vs.\ \(\SchemaCIZCorrEM\) (CIZ) -- consistent
with CIZ's ex-date dividend-reinvestment convention differing from SIZ's
month-end convention, but the qualitative conclusion is unchanged under
either schema.

\section{Our Own Exhibits}
\label{sec:exhibits}

Lewellen's entire argument depends on \(\log(DY)\) staying close enough to a
unit root that the \(\rho \approx 1\) conditional test has power -- he
states the requirement explicitly (Section 2.4): roughly 0.98 monthly
autocorrelation for a 25-year sample, 0.99 for 50 years. His own sample
ends in 2000, so the paper never asks whether that condition keeps holding.
We do, using the extended CIZ panel built for
Section~\ref{sec:extension}.

Table~\ref{tab:own-decades} reports mean, standard deviation, and lag-1
autocorrelation (\(\hat\rho\)) of \(\log(DY)\), \(\log(B/M)\), and
\(\log(E/P)\) by decade. \(\log(DY)\)'s persistence is not flat across the
sample: it stays high through the decades surrounding the paper's own
1946--2000 window, then declines in more recent decades, consistent with
the continuous picture in Figure~\ref{fig:own-rho}.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\input{\PathToOutput/own_summary_table.tex}%
}
\caption{Mean, standard deviation, and lag-1 autocorrelation of
\(\log(DY)\), \(\log(B/M)\), and \(\log(E/P)\) by decade, 1946--2025 (CIZ
panel). The takeaway is in the \(\hat\rho\) columns, not the levels: \(\log(DY)\)'s autocorrelation declines in recent decades while \(\log(B/M)\)
and \(\log(E/P)\) remain comparatively stable.}
\label{tab:own-decades}
\end{table}

Figure~\ref{fig:own-rho} makes the same point continuously rather than by
decade, and sharpens it. Rather than reporting where a rolling 25-year
\(\hat\rho\) for \(\log(DY)\) first happens to dip below the paper's own
power threshold -- a framing sensitive to whichever window is earliest and
noisiest, and not informative on its own -- the series has a clear shape:
it \textbf{peaks at \(\RhoPeakValue\) in \(\RhoPeakYear\), within the
closing years of Lewellen's own sample}, and has eroded steadily since,
to \(\RhoCurrentValue\) as of \(\RhoCurrentYear\). The conditions the
\(\rho \approx 1\) conditional test needs were at their most favorable
around the time the paper was written, and have been weakening ever since.
(A sample estimate above one is not a contradiction of the stationarity
assumption the test relies on: \(\hat\rho\) is an unbounded OLS slope, and
a near-unit-root process routinely yields \(\hat\rho > 1\) in a finite
sample even when the true \(\rho\) is below one. The paper's own Table~1
reports \(\rho_{24} = 1.062\) for \(\log(DY)\) for the same reason.)
(For reference, the series first dips below the \RhoThreshold{} threshold
around \RhoFirstCrossYear{}, but that single crossing is a much weaker
claim than the peak-to-current trajectory -- the series recrosses the
threshold repeatedly rather than declining monotonically, so a first-crossing
date alone would understate how much of the decline happened well after
that point.)

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{own_rho_evolution.png}
\caption{Rolling 25-year autocorrelation of \(\log(DY)\), 1946--2025 (CIZ
panel), against the paper's own power threshold for a window of that length
(Section 2.4). Persistence peaks at \(\RhoPeakValue\) in \(\RhoPeakYear\) --
within the closing years of the paper's own 1946--2000 sample -- and has
declined since to \(\RhoCurrentValue\) by \(\RhoCurrentYear\), its lowest
sustained level in the post-1946 sample: the conditions the conditional
test depends on were most favorable around the time Lewellen was writing.
The peak exceeding one reflects finite-sample variation in an unbounded
estimator, not a violation of stationarity.}
\label{fig:own-rho}
\end{figure}

The point of both exhibits is the same: the paper's conditional test is
powerful precisely because \(DY\) has historically been extremely
persistent, and that is an empirical fact about the twentieth-century
sample the paper studies, not a property guaranteed to hold indefinitely.
Whether it continues to hold is exactly the question a reader deciding
whether to trust the conditional test on \emph{current} data needs
answered, and it is not one Lewellen's own tables can address.

\FloatBarrier
\section{Conclusion}
\label{sec:conclusion}

\paragraph{Where it worked.} The dividend-yield replication is essentially
exact. Our OLS standard error for VWNY over 1946--2000 is 0.477 against the
paper's published 0.476 -- agreement to three decimal places on a statistic
that depends jointly on the index construction, the dividend-flow
reconstruction, and the sample being the same 660 months. Tables~1 through 3
reproduce with no failures against the tolerances fixed before we saw our
results, and the qualitative pattern the paper is built around survives the
extension to 2025: the unconditional test declines to reject where the
conditional test rejects decisively.

\paragraph{Where it was hard: the errors that looked like successes.} Every
substantive problem we hit shared a shape -- the pipeline produced plausible
output, and only a second, jointly-determined statistic revealed the fault.

An early version of the index aggregation computed the dividend yield's
denominator from lagged rather than current market value. This shifted the
entire \(DY\) series by one month. Every univariate statistic in Table~1 --
mean, standard deviation, skewness, autocorrelation -- remained correct, and
the QA gate passed. What failed was \(\mathrm{corr}(e,m)\), which collapsed
from its expected \(\approx -0.9\) to roughly \(-0.05\). Because that
correlation is what drives both the Stambaugh bias adjustment and the
\(\rho \approx 1\) test's variance reduction, the estimators still ran and
still returned numbers; they were simply answering a question about
misaligned data. We now assert \(\mathrm{corr}(e,m)\) explicitly in the test
suite for that reason.

\paragraph{Where the data would not cooperate.}
CRSP's legacy tables stopped being updated after 2024-12-31 while the current
schema runs a year further; the legacy path returns a short panel rather than
an error, so an extension built on it would have silently ended early and
looked complete. And the book-to-market construction diverged from the paper
by about 10\%, which we could only localize because \(E/P\) diverged by half
as much -- an error in the firm-level accounting pull would have moved both
equally, so the asymmetry pointed at the book-equity formula specifically,
and from there to deferred taxes.

\paragraph{What remains unresolved.} Excluding deferred taxes brings \(B/M\)
within 2\% of the published mean but leaves its long-lag persistence below
the paper's, and a residual gap of roughly 5\% remains in both Compustat
ratios. We report this rather than tuning it away: the paper specifies
neither the firm screen nor the book-equity formula, so beyond a certain
point the comparison stops being a test of our implementation and becomes a
guess about an unstated choice. The dividend-yield results, where the paper
\emph{is} specific, are where the replication should be judged.

\end{document}
